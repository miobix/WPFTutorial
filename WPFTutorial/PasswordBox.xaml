<Window x:Class="WPFTutorial.PasswordBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFTutorial"
        mc:Ignorable="d"
        Title="PasswordBox"  Height="350" Width="300">
    <StackPanel>
        <StackPanel Margin="10">
            <Label>Text:</Label>
            <TextBox />
            <Label>Password:</Label>
            <!--Simple, jsut a textbox to hide the input-->
            <PasswordBox />
        </StackPanel>
        
        <!--It can have properties like PasswordChar, and PasswordLenght-->
        <StackPanel Margin="10">
            <Label>Text:</Label>
            <TextBox />
            <Label>Password:</Label>
            <PasswordBox MaxLength="6" PasswordChar="X" />
        </StackPanel>

        <TextBlock TextWrapping="Wrap" Margin="10">Note: The password property is ot implemented as a dependendcy property, cannot bind to it</TextBlock>
    </StackPanel>
</Window>
